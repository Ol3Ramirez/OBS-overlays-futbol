{
  "name": "obs-overlays-futbol",
  "version": "1.0.0",
  "private": true,
  "description": "Overlays en vivo para futbol con OBS — perfiles independientes (original, SRYiyo)",
  "type": "commonjs",
  "scripts": {
    "format": "biome format --write .",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@playwright/test": "^1.49.0"
  }
}
